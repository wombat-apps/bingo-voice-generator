# Copy to signing.env and fill with your certificate
# Find your certificate with: security find-identity -v -p codesigning | grep "Developer ID"
APP_IDENTITY="Developer ID Application: Your Name (TEAM_ID)"
